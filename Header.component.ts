import { Component} from '@angular/core';


@Component({
  selector: 'app-header',
  templateUrl: './Header.component.html'
})

export class HeaderComponent {
// @Output() featureSelected = new EventEmitter<string>();  (using routerLink)
 // onSelect(feature: string) {
//    this.featureSelected.emit(feature);   (using routerLink)
//  }
}

